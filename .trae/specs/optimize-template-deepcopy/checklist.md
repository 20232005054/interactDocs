* [x] `SummaryTemplateMapper` 包含批量插入支持

* [x] `StructureTemplateMapper` 包含批量插入支持

* [x] `DocumentService.create_document` 成功复制了核心信息模板

* [x] `DocumentService.create_document` 成功复制了摘要模板

* [x] `DocumentService.create_document` 采用层级排序和映射算法成功复制了结构模板，且 `parent_id` 外键约束未报错

* [x] 创建新文档后，文档关联的新模板能正常提供结构数据，无空大纲 Bug

