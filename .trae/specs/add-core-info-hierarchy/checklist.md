* [x] `CoreInfoTemplate` 模型已正确配置 `parent_id` 字段和 `children` 关联

* [x] `DocumentCoreInfo` 模型已正确配置 `parent_id` 字段和 `children` 关联

* [x] 数据库表结构已成功添加新字段并执行变更

* [x] Pydantic Schemas 能够正确序列化/反序列化带有 `children` 列表的树形数据

* [x] 核心信息查询接口返回的数据结构正确呈现为父子嵌套形式

* [x] 核心信息的创建与更新逻辑能够正确验证和写入 `parent_id`

