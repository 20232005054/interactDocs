# Tasks
- [x] Task 1: 更新 `SummaryTemplateService` 中的提示词组装逻辑
  - [x] SubTask 1.1: 修改 `render_ai_content` 方法，优先获取 `custom_prompt`，其次获取 `default_prompt`。
  - [x] SubTask 1.2: 将 `source_data_map` 中的键值对以结构化的形式（如 Markdown）拼接到提示词之后。
- [x] Task 2: 更新 `StructureTemplateService` 中的提示词组装逻辑
  - [x] SubTask 2.1: 修改 `render_ai_content` 方法，优先获取 `custom_prompt`，其次获取 `default_prompt`。
  - [x] SubTask 2.2: 将 `source_data_map` 中的键值对以结构化的形式拼接到提示词之后。

# Task Dependencies
- 无