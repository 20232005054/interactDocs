* [x] `SummaryTemplateService.render_ai_content` 能正确将纯文本提示词与 `source_data_map` 拼接。

* [x] `StructureTemplateService.render_ai_content` 能正确将纯文本提示词与 `source_data_map` 拼接。

* [x] 生成的提示词优先使用 `custom_prompt`，无则使用 `default_prompt`。

* [x] 提交修改的提示词能使 AI 结合 `sources` 数据生成符合要求的内容。

