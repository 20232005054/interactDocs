* [x] `DocumentCoreInfo` 数据库模型包含了 `field_type`、`options` 和 `is_required` 列。

* [x] `CoreInfoBase` Schema 包含了 `field_type`、`options` 和 `is_required` 字段，且 `options` 支持可选或字典结构。

* [x] `apply_core_info_template` 能正确地从模板中复制 `field_type`、`options` 和 `is_required` 到实例化对象。

* [x] 数据库表结构已成功更新。

