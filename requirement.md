# InteractiveDocs Code Requirement

本文件是我在 InteractiveDocs 仓库内进行代码生成与修改时遵守的执行约定。

约定来源：
- `.kiro/steering/frontend-conventions.md`

说明：
- 当前 `.kiro` 中有明确内容的规范主要是前端开发规范，因此本文件以前端约束为主。
- 当任务涉及后端或前后端联动时，后端实现不得破坏前端既有接口契约、字段命名和交互方式。
- 若用户有明确指令，用户指令优先；若现有代码与本文件冲突，优先保持仓库一致性，再在必要时做最小范围修正。

## 1. 总体原则

- 先读现有实现，再改代码，不凭空重构。
- 改动以最小闭环为目标，避免顺手扩大范围。
- 新增逻辑必须与现有目录边界一致，不把职责混写到错误层级。
- 尽量复用现有工具、组件、类型和请求封装，不重复造轮子。

## 2. 前端目录边界

前端目录按以下职责使用：

- `src/app/`：仅放路由页面与布局，不直接写复杂业务逻辑或接口调用。
- `src/components/`：可复用 UI 组件，负责展示与交互，不承担数据访问职责。
- `src/containers/`：页面级数据获取、状态编排、业务串联。
- `src/services/`：所有后端接口访问，按业务域拆分。
- `src/store/`：Zustand 全局状态，只放跨组件共享状态。
- `src/hooks/`：复用型逻辑。
- `src/lib/`：纯工具函数与公共基础设施。
- `src/types/`：全局类型定义，特别是接口类型。

禁止事项：

- 在 `page.tsx` 内直接写接口请求或复杂业务编排。
- 在组件里直接写 `fetch` / `axios`。
- 在 store 内直接发起异步请求。

## 3. 接口与数据访问约定

- 所有常规请求必须通过 `src/services/` 暴露的函数发起。
- `axios` 统一走 `src/lib/request.ts`，复用拦截器、超时、错误处理与 token 注入。
- 不在业务代码里手动拼 `Authorization` 请求头。
- 不在组件层直接解析后端通用响应壳，统一由请求层或 service 层处理。
- 仅当共享请求层无法满足需求时，才允许局部绕过；必须有明确理由，例如超时策略、流式消费或 SSE。

## 4. 接口选择与交互策略

- 优先使用语义明确的细粒度接口，不用全量覆盖代替局部操作。
- 页面初始化涉及多个独立请求时，优先 `Promise.all` 并发。
- 已有 SSE 或流式协议的场景，不退回轮询。
- 对后端异步联动场景，优先保持前端响应速度，通过事件或后续拉取同步最终状态。

## 5. 状态管理约定

- Zustand 仅保存跨组件共享状态。
- 组件私有状态使用 `useState`、`useReducer` 等局部状态方案。
- store 负责存值和同步，不承担页面副作用编排。
- 禁止在 store 中引入组件，禁止制造循环依赖。

## 6. 组件与复用约定

- 只有在 UI、数据结构、交互行为都一致时才抽公共组件。
- 相同逻辑至少出现 2 次，再考虑抽 hook 或 util。
- 优先复用现有 shadcn 组件和项目内公共组件。
- 复用的目的是减少重复，不为了“看起来更抽象”而额外增加层级。

## 7. 样式约定

- 使用 Tailwind 作为主要样式表达方式。
- 不新增独立 `.css` 或 `.module.css`，除非仓库已有明确先例且当前模块必须沿用。
- 不写随意的魔法数字样式。
- 响应式优先使用 Tailwind 断点。
- 动态类名统一通过 `cn()` 一类工具处理，不手写易错字符串拼接。

## 8. 异步、错误与交互反馈

- 每个异步操作必须显式处理 `loading`、`error`、成功结果三种状态。
- 加载反馈优先使用局部 skeleton、spinner 或按钮级反馈，不轻易使用全屏遮罩。
- 删除、覆盖、重排等破坏性或不可逆操作，必须提供确认机制。
- 面向用户的错误文案使用中文，避免直接暴露底层异常细节。

## 9. 流式内容与 SSE

- AI 对话、AI 帮填、文档事件等流式能力，优先使用 SSE 或流式读取。
- 流式内容应渐进渲染，不等待完整响应后一次性展示。
- 需要重连时采用可控重试与退避策略。
- 收到后端变更事件后，只刷新受影响的数据，不做无差别全局重载。

## 10. TypeScript 与类型约定

- 函数参数、返回值、组件 props 必须显式建模。
- 默认禁止 `any`；确需使用时必须有局部理由，且范围尽量小。
- 后端接口类型统一维护在 `src/types/api.ts` 或其等价位置，并与后端字段名保持一致。
- 组件 props 优先使用 `interface`，与项目现有约定保持一致。

## 11. 文件命名约定

- 组件：`PascalCase.tsx`
- hooks：`useXxx.ts`
- services：`camelCase.ts`
- store：`camelCase.ts`
- utils / lib：`camelCase.ts`

## 12. 后端联动约定

当任务涉及后端时，遵守以下补充要求：

- 不随意修改前端已依赖的接口路径、字段名、响应包结构。
- 若必须调整接口契约，应同步更新前端 service 与类型定义，保持调用闭环。
- 优先保持接口语义稳定，避免同一路径承担多种不兼容行为。
- 新增接口时，命名、返回格式和错误处理方式应与现有 API 保持一致。

## 13. 执行方式

后续我在本仓库生成代码时，默认按以下顺序执行：

1. 先读取相关模块现有实现与依赖边界。
2. 只在对应职责层修改代码。
3. 若新增接口调用，优先补到 `services` 和类型定义，而不是直接写进组件。
4. 若新增共享状态，先判断是否真的需要进入 store。
5. 改动完成后，优先做最小必要验证，确认没有破坏现有契约。

## 14. 例外处理

- 若用户明确要求偏离此约定，我会按用户指令执行，并尽量把偏离范围限制在当前任务内。
- 若仓库已有实现与本约定不完全一致，优先保持已有模块风格，不做无关清洗式重构。
- 若后续 `.kiro` 新增更完整规范，应以新的 `.kiro` 规范更新本文件。
